const container = document.createElement('div');
container.classList.add('container');
document.body.appendChild(container); 

const grid = document.createElement('div');

// Set the id of the div to 'grid'
grid.id = 'grid';

// Optionally, append the grid to the body or another element
document.body.appendChild(grid); // Adds the div to the body of the document